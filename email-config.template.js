// EmailJS Configuration Template for Vue.js
// Copy this file to src/config/email.js and fill in your actual values

export const EMAIL_CONFIG = {
    // Get these values from your EmailJS dashboard
    PUBLIC_KEY: 'YOUR_PUBLIC_KEY_HERE',
    SERVICE_ID: 'YOUR_SERVICE_ID_HERE', 
    TEMPLATE_ID: 'YOUR_TEMPLATE_ID_HERE',
    
    // Optional: Customize the destination email in your EmailJS template
    // The template variables available are:
    // - from_name (first name)
    // - last_name (last name) 
    // - from_email (sender's email)
    // - message (the message content)
};
